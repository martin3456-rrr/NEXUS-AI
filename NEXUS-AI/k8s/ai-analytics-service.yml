apiVersion: apps/v1
kind: Deployment
metadata:
  name: ai-analytics-service
  namespace: nexus-ai
spec:
  replicas: 1
  selector:
    matchLabels:
      app: ai-analytics-service
  template:
    metadata:
      labels:
        app: ai-analytics-service
      annotations:
        prometheus.io/scrape: 'true'
        prometheus.io/path: '/actuator/prometheus'
        prometheus.io/port: '8082'
    spec:
      containers:
        - name: ai-analytics-service
          image: ghcr.io/your-username/nexus-ai/ai-analytics-service:latest
          ports:
            - containerPort: 8082
          envFrom:
            - configMapRef:
                name: global-config
          env:
            - name: SPRING_PROFILES_ACTIVE
              value: "kubernetes"
          livenessProbe:
            httpGet:
              path: /actuator/health
              port: 8082
            initialDelaySeconds: 90
            periodSeconds: 10
          readinessProbe:
            httpGet:
              path: /actuator/health
              port: 8082
            initialDelaySeconds: 60
            periodSeconds: 5
          resources:
            requests:
              memory: "512Mi"
              cpu: "500m"
            limits:
              memory: "1Gi"
              cpu: "1"
---
apiVersion: v1
kind: Service
metadata:
  name: ai-analytics-service-service
  namespace: nexus-ai
spec:
  selector:
    app: ai-analytics-service
  ports:
    - port: 8082
      targetPort: 8082
  type: ClusterIP
