server:
  port: 8081

spring:
  application:
    name: ai-analytics-service

management:
  endpoints:
    web:
      exposure:
        include: "health,info,prometheus"
  endpoint:
    health:
      show-details: always

eureka:
  client:
    service-url:
      defaultZone: http://localhost:8761/eureka/
    register-with-eureka: true
    fetch-registry: true
  instance:
    prefer-ip-address: true

# Logging (opcjonalne – dla debug rejestracji i predykcji)
logging:
  level:
    com.netflix.eureka: INFO
    com.nexus.analytics: DEBUG
