server:
  port: 8888
spring:
  application:
    name: config-server
  cloud:
    config:
      server:
        git:
          uri: https://github.com/twoj-uzytkownik/nexus-ai.git
          search-paths:
            - config-repo # Ścieżka do katalogu z konfiguracją
eureka:
  client:
    serviceUrl:
      defaultZone: http://localhost:8761/eureka/