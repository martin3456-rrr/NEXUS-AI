spring:
  application:
    name: user-service
  cloud:
    vault:
      host: vault.nexus-ai.svc.cluster.local
      port: 8200
      scheme: http
      authentication: KUBERNETES
      kv:
        enabled: true
  profiles:
    active: dev
